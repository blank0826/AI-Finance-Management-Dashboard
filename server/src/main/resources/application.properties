# Server
server.port=8080

# Database - defaults to H2 for local dev, override with env vars for production
# Database — PostgreSQL local
spring.datasource.url=jdbc:postgresql://localhost:5433/financedb
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable H2 console (no longer needed)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# File upload limits
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=50MB

# JWT
app.jwt.secret=G5uIZ5c2kQfVifbJhB5EXh3CFxnqTBE9RYjCROs2idw=
app.jwt.expiration-ms=86400000

# Cloudinary file storage
app.cloudinary.cloud-name=dg0f38imn
app.cloudinary.api-key=338812983147392
app.cloudinary.api-secret=NbMaaQ36jbUonMx7NcXlIFlgDNk
app.cloudinary.enabled=true

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}
